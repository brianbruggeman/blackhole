# QueryView corpus

This directory is reserved for minimized DNS wire samples produced by
`cargo fuzz run query_view`. Keep samples bounded and free of client identity,
credentials, and application payloads before committing them.

# Blackhole final gate

This is an evidence ledger, not a completion claim. A gate is marked proven
only for the exact command and scope that ran. Missing evidence remains open.

## Proven

| Gate | Evidence |
| --- | --- |
| Formatting | `cargo fmt --all -- --check` and `git diff --check` passed at Blackhole `f09174e` with Rust `1.98.0` and Cargo `1.98.0`. |
| Compilation | `CARGO_BUILD_JOBS=1 cargo check --locked --all-targets` passed at Blackhole `5092a05` with every Proxima dependency pinned to GitHub commit `03ea2c7d` (the portable-atomic circuit-breaker fix), Rust `1.98.0`, and Cargo `1.98.0`. |
| Unit and integration tests | `CARGO_BUILD_JOBS=1 cargo test --locked --workspace --all-targets --no-fail-fast -- --test-threads=1` passed at Blackhole `e1042de`: 150 library tests, 5 executable tests, 1 live admin fixture, 6 deployment-artifact tests, 4 resolver fixtures, 15 fake-upstream tests, and all example targets passed against GitHub Proxima commit `03ea2c7d`. |
| Cache failure behavior | `fake_upstream_servfail_is_not_cached` passes in the current 103-test run at Blackhole `c23c1ac`; SERVFAIL causes a second upstream exchange rather than a reusable negative cache entry. |
| Nextest | `CARGO_BUILD_JOBS=1 cargo nextest run --locked --all-targets --status-level fail --final-status-level fail` passed at Blackhole `65e8eba`: 188 tests, 188 passed, 0 skipped, across 9 binaries against GitHub Proxima commit `03ea2c7d`; the real admin, resolver, fake-upstream, recording, parser-cause, invalid-question, rewrite, deployment-artifact, listener-latency, ownership-recovery, country-status, region/ASN-policy, DDoS persistence, adaptive-abuse UI, metadata-replay, amplification-boundary, atomic full-config reload, atomic country-policy replacement, blocklist source management, native package artifact, and all-action contract fixtures pass. |
| Doctests | `CARGO_BUILD_JOBS=1 cargo test --locked --doc` passed at Blackhole `5092a05`: 1 doctest. |
| No-std and WASM | `CARGO_BUILD_JOBS=1 cargo build --locked --no-default-features --target thumbv7m-none-eabi` passed at Blackhole `5092a05` with GitHub Proxima commit `03ea2c7d`; the pure `blackhole::edge` path and reusable `EdgePolicy` remain buildable in the previously proven WASM artifact at `f8ebc00`. The target-gated WASM probe also validates a real query and rejects a truncated query through the Node harness. |
| WASM edge runtime probe | Three fresh disposable-target runs of `node scripts/wasm_edge_bench.mjs` against the `f8ebc00` `wasm32-unknown-unknown` artifact completed 100,000 probe calls each: module `5,565,632` bytes, linear memory `2,162,688` bytes, valid result `0`, short result `-1`, and `1,958.42..2,018.50 ns/call`; a fresh bounds run at `014e1d1` also returned `null=-1` and `oversized=-1`. The scalar production path is unchanged. |
| Cross-platform GitHub Actions | GitHub Actions run `33478035789` for `1f15c30` completed successfully for all five jobs: Linux, macOS, WASM, performance, and fuzz. Non-expired fuzz, macOS diagnostics, performance, and WASM provenance artifacts were published. |
| Tokio compatibility build | `CARGO_BUILD_JOBS=1 cargo check --locked --features tokio-compat --all-targets` passed at Blackhole `5092a05`; the lane compiles Tokio capability support while the default executable remains Prime-backed. |
| Dependency audit | Root `cargo audit` and `cargo audit --file fuzz/Cargo.lock` completed successfully against the current advisory database; both graphs report no vulnerability finding and only the allowed warning for unmaintained `paste` (`RUSTSEC-2024-0436`). The audit result was collected from Blackhole `151ddcc` and covers 359 root and 338 fuzz dependencies. |
| Fuzz smoke | `cargo +nightly fuzz run query_view --fuzz-dir fuzz -- -runs=1000 -rss_limit_mb=2048` completed successfully at Blackhole `16b33bd` with GitHub Proxima commit `03ea2c7d` and nightly Rust `1.100.0`: 1,000 executions, 56 seed inputs, 138 coverage features, 57-case run corpus, about 73 MiB RSS, and no crash. Generated corpus files were removed after the smoke run, restoring the checked-in 56-file corpus. The CI timeout is 300 seconds so a clean sanitizer build is included in the bounded gate. |
| Resolver fixture | The actual loopback UDP/TCP listener fixture passed. |
| Configuration validation | At Blackhole `f09174e`, `cargo run --locked -- --check blackhole.example.toml` exited successfully before listener startup; enabled capture plans are validated through the platform planner without installation. |
| Examples | `cargo check --locked --examples` passed at Blackhole `3be6042`. |
| Reproducible release bundle | `sh -n deploy/package/build.sh`, the focused package-builder contract test, and a real Linux archive build passed. The archive contained the executable, example configuration, systemd/launchd assets, `PROVENANCE.txt`, and `SHA256SUMS`; native package-manager artifacts and host upgrade automation are not claimed. |
| Native Debian package artifact | `sh -n deploy/package/build-deb.sh`, the deployment-artifact contract tests, and a real Linux package build passed. The `.deb` contains `debian-binary`, `control.tar.gz`, and `data.tar.gz`; its bounded lifecycle hooks create the service account/state directory and perform systemd reload/enable/start or stop/disable actions when available. Host installation and upgrade/rollback smoke verification are not claimed. |
| Authenticated admin control plane | Focused and real-listener tests prove Proxima HTTP health routing, bearer rejection/admission, bounded 404/405 routes, authenticated blocklist, country-map, complete domain-rule, incremental domain-rule append/upsert/removal, regex-rule reload and stable-ID upsert/removal, complete rewrite replacement plus upsert/removal and inspection, bounded policy bodies, live bounded `/rules` inspection, bounded effective-policy status projection including safe legacy fallback metadata, monotonic publication generation, and loopback-only binding while the control plane is plaintext HTTP; invalid/duplicate/unknown updates fail closed without publication, and the 155-test run passes at Blackhole `4e3520d`. |
| Authenticated admin status page | The bearer-authenticated Proxima admin pipe serves a bounded status UI under 4 KiB with status, rule metadata, privacy-log controls, and a policy-bundle editor that loads the live bundle and publishes through the existing atomic route; it contains no packet payloads and uses the existing Proxima HTTP pipe. Unit, configured-snapshot, and live-listener proofs pass at Blackhole `b4df82f`. |
| Authenticated adaptive-abuse controls | The status UI now loads `/abuse/status` and invokes authenticated `POST /abuse/clear` without exposing blacklist keys. The UI route-shape proof and complete all-target test gate pass at Blackhole `5b4484b`. |
| Authenticated profile, group, and identity administration | Bounded `GET /profiles`, `GET /client-groups`, and `GET /client-identities` routes expose configured metadata without runtime client identity. Authenticated bounded reload/upsert/removal routes atomically manage profiles, client groups, and exact/CIDR client identities; duplicate, unknown, invalid, and overlapping identity scopes fail without publication. The focused identity proof and complete 181-test all-target gate pass at Blackhole `e1042de`; richer policy controls remain open. |
| Background country-map lifecycle | Optional `country_policy.reload_interval_secs` composes Proxima's cancellable interval/lifecycle primitives, reloads only changed valid maps, records reload latency by kind, and preserves the last good map after a failed refresh. Map rows now support optional bounded region and ASN labels; explicit country, region, and ASN deny/observe selectors reject overlap across selector dimensions. The focused region/ASN, unchanged/changed, fail-closed, and cross-dimension-overlap proofs pass in the 155-test nextest run at Blackhole `4e3520d`. |
| Atomic policy-bundle administration | Authenticated bounded `POST /reload/policy-bundle` validates and replaces domain, regex, profile, client-group, local-rewrite, and country-policy tables as one policy snapshot. Optional `mode`, `domains`, and `default_action` fields replace the legacy fallback settings in the same publication; an optional blocklist-path array is loaded before publication and omitted/null retains the current snapshot. Invalid legacy domains retain the prior live settings. A shared publication lock serializes every reload writer with policy/action/status readers. |
| Incremental configuration-file reload | A bounded top-level `reload_interval_secs` enables a Proxima cancellable interval against the configured TOML file. It republishes policy tables and live admission limits through the existing atomic bundle path, reapplies environment overrides, and rejects listener, transport, storage, capture, service, capacity, persistence, or reload-interval changes. `configuration_reload_publishes_policy_and_rejects_startup_changes`, the example `--check`, no-std build, and the complete 185-test nextest gate pass at Blackhole `1fcbacf`, against GitHub Proxima `03ea2c7d`. |
| Exact client group identities | Named client groups accept bounded exact IPv4/IPv6 addresses alongside CIDRs; exact addresses remain narrow while CIDR scopes retain network matching. Duplicate addresses and expanded-rule overflow fail closed, authenticated group administration round-trips the address field, and focused policy/admin/resolver tests pass in the 154-test nextest run at Blackhole `a54aecc`. |
| Authenticated cache deletion | The bearer-authenticated `POST /cache/clear` operation removes bounded positive and negative cache entries and returns only the removed count; the focused route proof passes at Blackhole `5dc91e9`. Persistent query-log and honeypot retention controls remain separate gates. |
| Privacy-safe query decision log | The optional `privacy.query_log_enabled` sink is disabled by default, bounded by entry count and retention age, stores only Proxima action/qtype/qclass/timestamp metadata, caps authenticated `GET /logs` at 1,024 entries with a truncation flag, and supports `POST /logs/clear`. Unit, real authenticated-listener, and projection tests pass at Blackhole `ad5f3e0`. |
| Live forwarding FSM path | The real loopback resolver fixtures exercise the listener's `Matched(Forward)`, `Forward`, `Forwarded`, and response-send transitions while forwarding through Proxima's GitHub-pinned `DnsClientUpstream`; the TCP adapter also advances `BeginParse`/`NeedMore` for bounded partial frames before the TCP fallback fixture exercises the UDP-TC-to-TCP path. The full 141-test nextest run passes at Blackhole `46f68ed`. |
| TCP upstream fallback | `DnsClientUpstream` retains the echoed question and TC bit, validates `QR=1`/`OPCODE=0`, retries TC-marked UDP answers through the injected existing `StreamUpstream`, and exchanges bounded two-byte-length DNS-over-TCP frames under the same configured deadline. Proxima's deterministic fallback test and Blackhole's real listener fixture pass against GitHub Proxima `e1c91f7`. |
| Encrypted upstream transport | Blackhole's `udp`, `tcp`, `tls`, and `doh` upstream modes are wired to Proxima's existing DNS client and HTTP/TLS pipe adapters. The opt-in `doq` feature additionally wires DNS-over-QUIC through Proxima's GitHub `afca73c` `QuicUpstream` adapter, which uses a bounded reusable connection pool and replaces stale pooled connections. Proxima's 29-test package lane, including `quic_upstream_and_listener_exchange_one_stream`, proves the loopback TLS/QUIC stream path; encrypted modes require an explicit validated server name, and the default build remains Prime-backed and QUIC-free. Configuration validation, default all-target compilation, and `--features doq` compilation pass at Blackhole `853aa81`. |
| Capture lifecycle | Planner, install-failure rollback, verification-failure rollback, ownership, recovery, and cleanup tests pass at Blackhole `bddb401`. Capture is explicitly opt-in, wired through the shared controller, and native Linux cleanup targets only the exact journal-owned chain rather than deleting the shared table. |
| Capture journal validation | Persistent ownership records are rejected on empty/oversized/non-ASCII identifiers, zero ports, or trailing data before recovery; the deterministic invalid-record proof and the full 152-test suite pass at Blackhole `fc899d2`. |
| Privileged capture smoke | GitHub Actions run `33499699912` for Blackhole `507f023` completed the Linux and macOS native smoke steps successfully: Linux installed and removed a uniquely owned nftables table/chain through `CaptureController`, and macOS enabled PF for the ephemeral runner and installed/removed a uniquely owned anchor. The smoke harness verifies that its ownership journal is removed. |
| Linux deployment artifact | `deploy/systemd/blackhole.service`, its tmpfiles definition, and the root-checked `deploy/systemd/install.sh` are present and diff-validated; the unit restricts the service to a dedicated user, low-port bind capability, and `/var/lib/blackhole` state. The installer’s non-root guard exits before mutation, and its upgrade transaction backs up/restores the installed files and restores the prior active-service state on failure. Shell syntax and the deployment-artifact tests pass at Blackhole `428eb04`. |
| Host-installed systemd and resolver smoke | Read-only host inspection found `blackhole.service` enabled and active as `blackhole:blackhole` in `/system.slice/blackhole.service`, with root-owned `0755` binary, `blackhole:blackhole` `0640` config, root-owned unit/tmpfiles files, and a `0750` state directory. `sudo /usr/local/bin/blackhole --check /etc/blackhole/blackhole.toml` passed; UDP and TCP `dig` queries to `127.0.0.1:5353` both returned `NOERROR`; installed and local optimized release binaries matched SHA-256 `6481cf4442cbe6f23ed8e2a042e31ed60b08784d34c0ba61377426eb78c3eda2`. No installation or service mutation was performed during this inspection. |
| macOS launchd deployment artifact | `deploy/launchd/com.brianbruggeman.blackhole.plist` runs the resolver directly as `_blackhole`, applies bounded process/file limits, and performs no PF mutation. The checked-in installer validates the release and plist, installs ownership-scoped files, and rolls back an interrupted upgrade. XML validation, shell syntax, formatting, compile, and the six deployment-artifact tests pass at Blackhole `0e931fe`; macOS CI now also syntax-checks both installers and runs the real loopback resolver fixture, while the last completed run `33478035789` predates those checks and proves only the plist validation. |
| Disposable-root systemd verification | `sudo -n env BLACKHOLE_BINARY=... BLACKHOLE_CONFIG=... deploy/systemd/smoke.sh` passed at Blackhole `f7d52bd`: the real installer completed a root-owned disposable install with copied systemd targets, then a forced missing-target verification failure restored pre-existing binary/config/unit/tmpfiles files byte-for-byte. The fixture cleaned both temporary roots and did not mutate the host service. |
| Per-client admission | The configured per-client concurrent-request cap rejects a second in-flight request at the limit, releases on completion, and passes the focused unit test. |
| Response amplification cap | The configured response-ratio cap is validated and applied below the absolute response ceiling; a bounded aggregate encoded-egress budget sheds total responses, including unidentified callers, before transport write. A response reaching the ratio ceiling now emits `response_amplification_cap` and contributes to the existing client/network temporary abuse breaker. Focused boundary coverage and the complete 182-test nextest gate pass at Blackhole `06958f1`. |
| Per-client rate limiting | The configured per-second client rate cap sheds the third request in a deterministic focused test, while unidentified callers remain unkeyed and the rate-state table is bounded. |
| Atomic blocklist reload | A replacement blocklist publishes as one snapshot and an invalid replacement leaves the previous generation active; the focused reload test passes in the current 89-test library suite. |
| Background blocklist reload | `policy.blocklist_reload_interval_secs` is opt-in, bounded to one day, composed through Proxima's cancellable interval/source lifecycle, skips unchanged content without advancing `policy_generation`, and drains at shutdown; bounded configuration and unchanged/changed reload proofs pass at Blackhole `b275b9a`, with 130/130 nextest tests passing. |
| Bounded blocklist sources | Blocklist reloads bound source count, path length, each file, aggregate file bytes, line length, and generated domain count before publication; deterministic count/path tests and the 127-test workspace nextest run pass at Blackhole `c61eaf4`. |
| Blocklist authority across reloads | Complete rule and profile publication preserves the currently loaded blocklist snapshot; a deterministic reload test proves the blocklisted name remains blocked after policy replacement at Blackhole `b08604e`. |
| Cache outcome telemetry | Proxima-native counters record fresh hits, misses, stale serves, and capacity evictions; the deterministic bounded-eviction test passes. |
| Cache TTL telemetry | The focused `cache_ttl_telemetry_reports_effective_positive_and_negative_ttls` test proves the histogram reports the configured post-clamp positive TTL and negative TTL through Proxima's telemetry interface. |
| Listener parser failure telemetry | The borrowed query boundary maps every parser result to a bounded stable cause label, and the actual listener `decide` path emits `query_wire_short` through Proxima telemetry; the full 141-test nextest run passes both proofs at Blackhole `a1de0bc`. Attacker-controlled parser text is not used as a metric label. |
| Listener latency telemetry | The listener records `blackhole.listener_latency_ns` through the existing Proxima telemetry surface on every `decide` exit, including malformed-parser drops; the deterministic parser-drop histogram proof and full 151-test suite pass at Blackhole `210ac3e`. |
| Reload latency telemetry | `telemetry_records_reload_latency_by_reload_kind` proves Proxima's histogram surface receives successful rules and regex reload durations labeled by reload kind at Blackhole `3bd1062`. |
| Local DNS rewrites | Bounded A/AAAA rewrites answer pass/observe queries, explicit policy actions override them, and malformed or oversized configuration fails closed. |
| Per-client abuse breaker | Full all-target suite passed with a focused test covering repeated rate-limit violations opening a temporary bounded breaker while unidentified callers remain unaffected. |
| Network abuse breaker | Focused coverage proves repeated violations from clients in one configured IPv4/IPv6 network open a bounded network breaker while another network and unidentified callers remain unaffected. |
| Per-client response-byte budget | Focused tests prove encoded-response budget accumulation sheds a client at the configured one-second ceiling, unidentified callers remain unkeyed, and zero-value configuration fails closed. |
| Adaptive response-budget abuse breaker | The actual listener now records identified-client response-budget violations in the existing bounded abuse state; repeated violations trigger its configured cooldown breaker, while unidentified callers remain unaffected. Focused coverage passes. |
| Global query-rate DDoS stopgap | The configured bounded `admission.max_queries_per_second` gate applies before policy/upstream work to both identified and unidentified callers, rejects invalid zero/over-limit configuration, and emits the distinct `global_rate_overflow` Proxima failure cause. Deterministic focused coverage and the 126-test workspace nextest run pass at Blackhole `79944ef`. |
| Network response-byte budget | The listener applies a bounded encoded-response-byte ceiling across each configured IPv4/IPv6 prefix after the global and per-client budgets; focused tests prove same-network shedding, unrelated-network isolation, anonymous bypass to the global budget, and zero-limit rejection at Blackhole `a92a6b9`. |
| Authenticated admission-limit status | The existing Proxima admin UI and bearer-authenticated `GET /admission/status` expose only configured query, response, amplification, and abuse limits; the route, UI link, wrong-method, and privacy-shape checks pass in the 150-test suite at Blackhole `d54112d`. |
| Live admission reload | Bearer-authenticated `POST /reload/admission` publishes validated rate, response-budget, and abuse-breaker limits through the live policy snapshot; `max_inflight_requests` remains startup-only and rejected changes leave the prior snapshot untouched. The admin route and runtime proofs pass in the 157-test workspace run at Blackhole `7577cf7`. |
| Authenticated country-policy status | The existing Proxima admin UI and bearer-authenticated `GET /country/status` expose bounded deny/observe controls, map-entry count, freshness, and reload interval without source paths or client addresses; focused and full-suite checks pass at Blackhole `eddd793`. |
| Named service profiles | Focused tests prove profile domains compile into authoritative rules, optional client CIDR scopes apply only to matching clients, and duplicate profile names or invalid domains fail closed. |
| Named client groups | `client_groups` provide bounded reusable IPv4/IPv6 CIDR sets for service profiles; policy tests prove one profile expands across multiple groups and unknown/mixed scopes fail closed, while the real loopback UDP listener fixture proves the adapter-to-policy path at Blackhole `96c08e1`. |
| Listener rewrite path | Full all-target suite includes a real loopback UDP test proving a local rewrite is encoded and returned through the Proxima listener adapter. |
| Listener profile path | Full all-target suite includes a real loopback UDP test proving a configured service profile is enforced by the listener's actual policy path. |
| Cache TTL bound | Full all-target suite includes tests proving positive and negative cache entries remain bounded, protocol TTLs above the configured ceiling are clamped, and a zero ceiling fails configuration validation. |
| Cache invalidation | Rule-table reload tests prove successful policy publication clears cached forwarding answers while failed reloads retain the previous snapshot. |
| Concurrent snapshot readers | `concurrent_readers_observe_only_complete_generations` runs four readers across 63 reloads and proves each read sees a complete generation, not a mixed rule table, at Blackhole `dcee4fd`. |
| Bounded upstream exchange | Current tree validates `query_timeout_ms` to 1–60,000 ms, `max_attempts` to 1–8, and `max_outstanding` to 1–4096 before listener construction; UDP and TCP upstream operations share the configured exchange deadline, and upstream answers reject impossible RCODE values above DNS’s 4-bit range. Focused validation and the full all-target suite pass against GitHub Proxima `d8cf174e`. |
| Upstream CNAME validation | Upstream CNAME targets are parsed through Proxima’s DNS name parser and rejected on pointer loops, trailing bytes, or invalid names; valid targets pass. Focused and full all-target tests pass. |
| Upstream loop prevention | Upstream configuration rejects exact listener endpoints and same-family unspecified listener binds that overlap the upstream address and port; IPv4 and IPv6 proofs pass. |
| Bounded upstream records | Upstream answers exceeding the configured `max_response_records` are rejected before validation can reach cache insertion; focused and full all-target suites pass. |
| Upstream pass-through | `pass` and `observe` actions use the configured upstream after local rewrites, while explicit `forward` remains distinct and fail-closed without an upstream; deterministic fake-upstream proofs pass. |
| DNS query flag validation | The shared borrowed query boundary and owned policy admission reject AA, TC, RA, Z, and nonzero RCODE bits, plus zero-valued qtype/qclass fields, before policy evaluation; focused and full all-target suites pass at Blackhole `09ec41e` with 145 tests, 145 passed. |
| Action contract | The executable policy proof covers all nine actions: local pass/observe use empty `NOERROR` without an upstream, ignore/drop/forward produce no owned answer, reject returns `REFUSED`, nxdomain returns `NXDOMAIN`, sink returns empty `NOERROR`, and honeypot returns its synthetic record; the focused proof passes at Blackhole `4d8113c`. |
| Unsupported IDNA policy | Configuration rules with non-ASCII/Unicode names and malformed DNS labels are rejected explicitly until an IDNA and confusable-name policy is implemented; the deterministic fail-closed proof passes at Blackhole `42b1fd7`. |
| Blocklist exceptions and subdomains | Bounded blocklist ingestion generates apex and subdomain rules, honors basic AdGuard `@@||domain^` exceptions, and proves normalization/deduplication and fail-closed parsing in the full suite. |
| Authenticated status endpoint | The existing Proxima bearer-authenticated admin handler exposes bounded non-sensitive status fields; unit and real HTTP listener proofs pass. |
| Authenticated rule inspection | The bearer-authenticated `GET /rules` route returns policy metadata without query payloads and caps serialized output at 64 KiB; focused tests prove non-empty output, truncation, and wrong-method rejection, while `admin_http_listener_enforces_bearer_auth` proves the route through a live Proxima HTTP listener at Blackhole `e9e4f3e`. |
| Privacy-safe decision recording | The optional Proxima `RecordingSink` hook emits only action, qtype, and qclass metadata; `recording_sink_receives_only_dns_decision_metadata` proves names and wire data are excluded at Blackhole `a5cde62`. |
| Configured Proxima JSONL decision recording | The executable's configured `privacy.query_recording_path` composes Proxima's `LazyFanOut`, `AccumulatingSink`, and an exact encoded-byte ceiling; `proxima_jsonl_recording_path_persists_metadata_only` writes a JSONL event while proving the query name is absent. Optional startup rotation retains a bounded number of numbered generations and verifies deletion of the exact oldest generation; the focused rotation proof and 154-test nextest pass at Blackhole `a54aecc`. This is not honeypot retention. |
| Durable DDoS incident recording and restart recovery | With `[admission.ddos].persist_incidents = true`, a threshold-crossing incident is appended through the existing bounded Proxima JSONL sink and `sync()` reaches the durable terminal before the request returns; startup reads the same source and restores only unexpired markers to exact-client and configured-network breakers. Configuration, metadata-only persistence, active/expired restoration, actual Proxima JSONL startup loading, and the complete 180-test all-target gate pass at Blackhole `1cb6f29`, against Proxima GitHub commit `03ea2c7d`. |
| Bounded metadata replay | `blackhole --replay recording.jsonl` consumes Proxima's existing JSONL source, rejects non-Blackhole/payload event kinds, enforces a 64 MiB file bound, and emits deterministic action and DDoS incident counts. The focused replay test, actual command fixture run, and complete all-target gate pass at Blackhole `f09174e`. |
| Durable recording deletion | `blackhole --delete-recording recording.jsonl` preflights and removes only the active Proxima JSONL recording plus its bounded `.1` through `.16` rotations, then verifies exact absence. The deletion proof, actual temporary-file CLI run, and complete 186-test nextest gate pass at Blackhole `15a5d37`, against GitHub Proxima `03ea2c7d`. |
| Authenticated privacy status | The Proxima bearer-authenticated `GET /privacy/status` projection exposes only recording enablement, bounds, and disabled payload/client-identity recording; the route and wrong-method proof pass in `health_and_unknown_routes_are_bounded`. |
| Admin UI privacy and reload controls | The bounded authenticated UI loads `/privacy/status` and the live `/policy-bundle` projection, and exposes authenticated blocklist and complete policy-bundle reload controls through the existing Proxima HTTP pipe; `health_and_unknown_routes_are_bounded` proves the UI endpoint, valid default bundle, and wrong-method rejection at Blackhole `3c6eb44`, while `admin_http_listener_enforces_bearer_auth` proves the configured bundle through the live listener at `80890e6`. |
| Authenticated blocklist source management | The bearer-authenticated `POST /reload/blocklists/replace`, `/add`, and `/remove` routes atomically manage exact source paths; `blocklist_source_replacement_is_atomic` proves publication, add/remove updates, and rollback after invalid or unknown paths. The complete 183-test nextest gate passed at Blackhole `0e20101`, against GitHub Proxima `03ea2c7d`. |
| Bounded regex policy rules | Regex expressions compile with count, pattern, and program-size bounds; invalid expressions fail closed, filter specificity is deterministic, explicit domain rules win, and focused policy proofs pass. |
| Regex client-network scopes | Regex rules accept bounded IPv4/IPv6 CIDR scopes, reject invalid or conflicting scopes, rank the most-specific matching network, expose metadata through `/rules`, and pass deterministic matching tests at Blackhole `a5cde62`. |
| Multi-network client scopes | Existing rule configuration accepts one bounded `client_cidrs` list of IPv4/IPv6 networks, rejects ambiguous or oversized scopes, and selects the longest matching prefix; focused policy proofs pass. |

## Evidence still required

| Gate | Current state |
| --- | --- |
| Performance gate | A fresh release run at Blackhole `f0185df`, GitHub Proxima `03ea2c7d`, Rust `1.98.0`, Linux x86_64, load average `1.36`, and `N=25` samples measured build, match, edge, short/long/adversarial/mixed parse, scalar/chunked/memchr name scans, owned conversion, response encoding, throughput, CoV, allocations, allocation bytes, boundary copies, and RSS. Borrowed parse/name-scan arms allocated zero times; owned conversion and response encoding measured their allocations. Edge p50 was 50,034 ns and RSS was 7,332 KiB. Scalar remains the production arm: scalar name scanning measured 59 ns p95, chunked 104 ns p95, and memchr 51 ns p95 but a 1,352 ns p99 with 3.420672 CoV. The WASM edge probe is measured separately; additional runtimes/workload cells and SIMD evidence remain open, and no zero-copy or production-performance claim is supported. |
| Privacy and honeypot terminal | The current metadata-only query log has bounded retention, authenticated access, and deletion. A separate durable honeypot terminal remains prohibited until the stronger retention, redaction, access-control, deletion-verification, consent, and restart tests in `PRIVACY.md` are implemented. |
| Managed GeoIP/region/ASN policy | Partial. The implementation supports an explicit operator-supplied country-to-CIDR map with region/ASN labels, fail-closed freshness, background refresh, and authenticated atomic selector/map replacement; managed database lifecycle and region/ASN sources are not implemented. `country_map_denies_and_observes_adapter_owned_clients` now also proves changed background maps advance `policy_generation` while unchanged maps do not; the complete 188-test nextest gate passes at Blackhole `32c5825`, against GitHub Proxima `03ea2c7d`. |
| Incumbent parity | Pi-hole/AdGuard parity is incomplete: richer admin/API/UI, richer identity policy controls, rotation/deletion-verified query-log backends, and managed GeoIP/region/ASN remain open; DHCP, DNS-over-TLS/HTTPS upstreams, opt-in DoQ wiring, named service profiles, atomic domain/regex/rewrite/profile/client-group/client-identity/blocklist/country administration, the bounded metadata-only query log, and a byte-bounded Proxima JSONL destination are implemented, while only the explicitly listed fixtures are claimed as tested. |

The open rows must be resolved and rerun with fresh evidence before this file
can support a release or a completion claim.
